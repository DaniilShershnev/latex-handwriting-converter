{
  "name": "handwriting-latex-converter",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && mkdir -p public/wasm && cp -f frontend/public/wasm/seshat.wasm public/wasm/ 2>/dev/null || true && npm run build",
    "start": "node api/index.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
